services:
  - type: web
    name: mystore
    env: node
    buildCommand: npm install && cd backend && npm install && cd .. && npm run build
    startCommand: node backend/server.js
    envVars:
      - key: NODE_ENV
        value: production
